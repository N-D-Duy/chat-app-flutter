import 'package:flutter/material.dart';

class ChatGroupCard extends StatelessWidget {
  const ChatGroupCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
